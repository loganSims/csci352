#include "btree.h"
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main (int argc, char** argv) {

  printf("page size = %lu\n", sizeof(struct Node));
  printf("data size = %lu\n", sizeof(struct Data));
  return 0;
}
